{
  "name": "github-webhook-handler",
  "version": "1.0.0",
  "private": "true",
  "main": "index.js",
  "repository": "https://github.com/opensumi/github-webhook-handler.git",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "build": "node build.js",
    "dev": "run-p dev:build dev:worker",
    "dev:build": "yarn build --watch",
    "dev:worker": "wrangler dev",
    "publish": "yarn build && wrangler publish"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^2.2.2",
    "@cloudflare/wrangler": "^1.19.0",
    "@types/itty-router-extras": "^0.4.0",
    "@types/node": "^16.7.10",
    "esbuild": "^0.12.17",
    "minimist": "^1.2.5",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@octokit/webhooks": "^9.22.0",
    "dotenv": "^10.0.0",
    "itty-router": "^2.3.10",
    "itty-router-extras": "^0.4.2"
  }
}
